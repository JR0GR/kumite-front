import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.kumite',
  appName: 'kumite',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
